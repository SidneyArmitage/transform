import { Program } from "../program/program";
import { command_instruction } from "./types/in/command";

export const process = (command: command_instruction, program: Program, message_id: number) => {
  
}